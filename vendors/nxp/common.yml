header:
  version: 14

repos:
  meta-freescale:
    url: "https://github.com/Freescale/meta-freescale.git"
    path: "layers/meta-freescale"
    layers:
      .:

  meta-freescale-3rdparty:
    url: "https://github.com/Freescale/meta-freescale-3rdparty.git"
    path: "layers/meta-freescale-3rdparty"
    layers:
      .:

  meta-freescale-distro:
    url: "https://github.com/Freescale/meta-freescale-distro.git"
    path: "layers/meta-freescale-distro"
    layers:
      .:

  meta-nxp-demo-experience:
    url: "https://github.com/nxp-imx-support/meta-nxp-demo-experience.git"
    path: "layers/meta-nxp-demo-experience"
    layers:
      .:

  meta-imx:
    url: "https://github.com/nxp-imx/meta-imx.git"
    path: "layers/meta-imx"

local_conf_header:
  nxp: |
    ACCEPT_FSL_EULA = "1"
    DISTRO_FEATURES += "opengl vulkan seccomp"
    USER_CLASSES ?= "buildstats"
    PATCHRESOLVE = "noop"
    PACKAGECONFIG:append:pn-qemu-system-native = " sdl"

bblayers_conf_header:
  nxp: |
    BSPDIR := "${@os.path.abspath(os.path.dirname(d.getVar('FILE', True)) + '/../..')}"

target:
  - imx-image-core  
  - imx-image-multimedia
  - imx-image-full

# artifacts:
#   imx-image-core.wic.zst: tmp/deploy/images/*/imx-image-core-*.wic.zst
#   imx-image-core.wic.bmap: tmp/deploy/images/*/imx-image-core-*.wic.bmap
#   imx-image-multimedia.wic.zst: tmp/deploy/images/*/imx-image-multimedia-*.wic.zst
#   imx-image-multimedia.wic.bmap: tmp/deploy/images/*/imx-image-multimedia-*.wic.bmap
#   imx-image-full.wic.zst: tmp/deploy/images/*/imx-image-full-*.wic.zst
#   imx-image-full.wic.bmap: tmp/deploy/images/*/imx-image-full-*.wic.bmap
